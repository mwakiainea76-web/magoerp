Refactor my sidebar navigation config from one large object into per-role
files, so adding/editing a role's nav doesn't require touching a giant
shared file.

CONTEXT:
- Current file: src/support/sidebarNavigationByRole.js (or wherever it
  lives) exports a single object `sidebarNavigationByRole` with keys
  admin, trainer, student — each an array of nav items (some with
  `children`, some flat with `to`).
- It also exports `getSidebarLinks(role)` which reads from that object
  with a fallback to `student`.
- Icons come from lucide-react. Dashboard paths come from
  `dashboardPathByRole` in "@/support/dashboardPaths".

WHAT TO DO:

1. Create a new folder: src/support/navigation/
   Split the object into one file per role, preserving every existing
   label, `to` path, icon, and `children` structure EXACTLY as-is (no
   renaming labels, no changing paths, no reordering items unless it's
   clearly grouping-related):
   - src/support/navigation/admin.nav.js   -> export const adminNav = [...]
   - src/support/navigation/trainer.nav.js -> export const trainerNav = [...]
   - src/support/navigation/student.nav.js -> export const studentNav = [...]

2. Each file must:
   - Import only the lucide-react icons it actually uses (no unused
     imports, no missing imports)
   - Import `dashboardPathByRole` only if that role's file references it
     for its Dashboard link
   - Export a single named array constant matching the pattern above

3. Create src/support/navigation/index.js that:
   - Imports adminNav, trainerNav, studentNav from the files above
   - Exports `sidebarNavigationByRole = { admin: adminNav, trainer:
     trainerNav, student: studentNav }`
   - Exports `getSidebarLinks(role)` with the same fallback-to-student
     behavior as the original

4. Update any file currently importing `sidebarNavigationByRole` or
   `getSidebarLinks` from the old location to import from
   `@/support/navigation` instead (search the codebase for both names
   first to find every usage).

5. Delete the old file once nothing else imports it.

6. Keep this extensible: adding a future role (e.g. "parent") should only
   require creating src/support/navigation/parent.nav.js and adding one
   line `parent: parentNav` inside index.js — no other files should need
   changes.

CONSTRAINTS:
- Do not change any `to` path strings.
- Do not change any label text or icon assignment.
- Do not change the shape of nav item objects (keep `children`,
  `defaultOpen`, etc. exactly as structured now).
- Do not introduce a new nav data format — keep the same array-of-objects
  shape already in use by whatever component renders the sidebar.
- After the refactor, list every file you created/modified/deleted so I
  can review the diff.

